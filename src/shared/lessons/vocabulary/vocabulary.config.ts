import { createLessonTheme } from '@shared/utilities/config/lesson-theme'
import { BookOpenText } from 'lucide-react'

export const VOCABULARY_CONFIG = createLessonTheme({ icon: BookOpenText, color: '#0f766e' })
